const websocketServer = require('../../../../websocket-server');

module.exports = async () => {
  // WebSocket server is initialized when this file is loaded
  console.log('WebSocket server has been started');
};

